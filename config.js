/* Your real config — fill in the two values below.
   (This file is git-ignored so it won't be committed.) */
window.CM_CONFIG = {
  cloudinaryCloud: "dtzubrdee",
  cloudinaryPreset: "marketplace listings",
};
