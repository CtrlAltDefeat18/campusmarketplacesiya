{
  "name": "campus-marketplace-makhanda",
  "version": "1.0.0",
  "private": true,
  "description": "Tailwind build pipeline for Campus Marketplace · Makhanda",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./static/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./input.css -o ./static/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
